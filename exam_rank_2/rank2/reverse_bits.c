#include <stdio.h>

unsigned char reverse_bits(unsigned char octet)
{
    int i;
    int j;

    i = 0;
    j = 8;
    while (i < 8)
    {
        a
    }
}

int main()
{
    unsigned char a = 5;

    printf("before - %c\n", a);
    printf("after - %c\n", reverse_bits(a));
}